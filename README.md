# Introduction

This repository holds the code for my image downloading extension.

# Getting started

Run `pnpm install` in the root directory.

To build for Firefox run `pnpm build:firefox`. The compiled files will be put in the **dist**-folder.